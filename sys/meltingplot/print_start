; print_start - forwarding file for the slicer start G-code.
; 3.7 renamed it to sys/meltingplot/print/prepare.g; the slicer start G-code and every
; file sliced before 3.7 still call this path. Kept, and silent for now - it becomes
; DEPRECATED with RRF 3.8 (then it warns with M118 P0 L1) so the slicer profiles can be
; moved to M98 P"0:/sys/meltingplot/print/prepare.g" first
; (CLAUDE.md, "Deprecated forwarding files").

M98 P"0:/sys/meltingplot/print/prepare.g"
