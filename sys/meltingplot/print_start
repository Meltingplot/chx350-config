; print_start - DEPRECATED forwarding file, delete with RRF 3.8
; (CLAUDE.md, "Deprecated forwarding files").
; 3.7 renamed it to sys/meltingplot/print/prepare.g.
; The slicer start G-code and every file sliced before 3.7 call the old path - change
; the slicer profile to M98 P"0:/sys/meltingplot/print/prepare.g".
; Every call warns on the console and in the event log (M118 P0 L1, with the profile
; name where the call carries it), so the remaining callers can be found and fixed
; before 3.8.

M118 P0 L1 S"Warning: deprecated print_start - slicer start G-code: print/prepare.g"
M98 P"0:/sys/meltingplot/print/prepare.g"
