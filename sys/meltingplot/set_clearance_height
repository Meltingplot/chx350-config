if !exists(global.clearanceHeight)
  global clearanceHeight=5

if exists(param.S)
  set global.clearanceHeight=min(move.axes[2].max,param.S)
else
  set global.clearanceHeight=5

if global.clearanceHeight > 400
  M291 P"Caution! The printed object may stick out of the printer in sequential printing, make sure there is enough space above the printer!" R"Large clearance height" S2